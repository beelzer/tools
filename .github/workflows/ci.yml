name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint & Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run lint
      - run: npm audit --audit-level=critical

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run check

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Vitest's browser project drives real Chromium via Playwright. The e2e
      # job has its own playwright-install step; we duplicate here so unit-test
      # can run independently when needed.
      - name: Get Playwright version
        id: pw-version
        run: echo "version=$(npm ls @playwright/test --json | jq -r '.dependencies["@playwright/test"].version')" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ steps.pw-version.outputs.version }}

      - run: npx playwright install chromium --with-deps
        if: steps.playwright-cache.outputs.cache-hit != 'true'

      - run: npx playwright install-deps chromium
        if: steps.playwright-cache.outputs.cache-hit == 'true'

      - run: npm run test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage/lcov.info
          flags: unit
          # Public repos can upload tokenless, but adding the token keeps us
          # under per-repo rate limits and unlocks PR status checks.
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  build:
    name: Build
    needs: [lint, typecheck, unit-test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build

      - uses: actions/upload-artifact@v7
        with:
          name: build-output
          path: dist/
          retention-days: 1

  e2e-test:
    name: E2E Tests
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Get Playwright version
        id: pw-version
        run: echo "version=$(npm ls @playwright/test --json | jq -r '.dependencies["@playwright/test"].version')" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ steps.pw-version.outputs.version }}

      - run: npx playwright install chromium --with-deps
        if: steps.playwright-cache.outputs.cache-hit != 'true'

      - run: npx playwright install-deps chromium
        if: steps.playwright-cache.outputs.cache-hit == 'true'

      - uses: actions/download-artifact@v8
        with:
          name: build-output
          path: dist/

      - run: npm run test:e2e

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      # Visual e2e report artifacts. Populated during `test:e2e` by the
      # report() helper in e2e/fixtures.ts. Download + `npm run test:report`
      # locally to view the HTML report.
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-artifacts
          path: test-artifacts/
          retention-days: 7
          if-no-files-found: ignore

  lighthouse:
    name: Lighthouse
    needs: [build]
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - uses: actions/download-artifact@v8
        with:
          name: build-output
          path: dist/

      - uses: treosh/lighthouse-ci-action@v12
        id: lhci
        with:
          configPath: lighthouserc.cjs
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: Post Lighthouse comment
        uses: actions/github-script@v9
        with:
          script: |
            const fs = require('fs');

            const badge = (score) => {
              if (score === null || score === undefined) return '`N/A`';
              const v = Math.round(score * 100);
              if (v >= 90) return `\`🟢 ${v}\``;
              if (v >= 50) return `\`🟠 ${v}\``;
              return `\`🔴 ${v}\``;
            };

            const ms = (value) => {
              if (value === undefined) return '—';
              return value >= 1000 ? `${(value / 1000).toFixed(1)}s` : `${Math.round(value)}ms`;
            };

            const manifest = JSON.parse(fs.readFileSync('.lighthouseci/manifest.json', 'utf8'));
            const links = JSON.parse('${{ steps.lhci.outputs.links }}');

            let rows = '';
            for (const entry of manifest) {
              const url = new URL(entry.url).pathname;
              const s = entry.summary;
              const reportUrl = links[entry.url] || '';
              const page = `[${url}](${reportUrl})`;

              const lhr = JSON.parse(fs.readFileSync(entry.jsonPath, 'utf8'));
              const a = lhr.audits;
              const fcp = ms(a['first-contentful-paint']?.numericValue);
              const lcp = ms(a['largest-contentful-paint']?.numericValue);
              const tbt = ms(a['total-blocking-time']?.numericValue);
              const cls = a['cumulative-layout-shift']?.numericValue?.toFixed(3) ?? '—';

              rows += `| ${page} | ${badge(s.performance)} | ${badge(s.accessibility)} | ${badge(s['best-practices'])} | ${badge(s.seo)} | ${fcp} | ${lcp} | ${tbt} | ${cls} |\n`;
            }

            const body = `## Lighthouse Report

            | Page | Performance | Accessibility | Best Practices | SEO | FCP | LCP | TBT | CLS |
            |------|:-----------:|:-------------:|:--------------:|:---:|:---:|:---:|:---:|:---:|
            ${rows}`.replace(/^            /gm, '');

            const marker = '## Lighthouse Report';
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes(marker)
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

  commitlint:
    name: Commit Messages
    if: github.event_name == 'pull_request' && github.event.pull_request.user.login != 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npx commitlint --from ${{ github.event.pull_request.base.sha }} --to ${{ github.event.pull_request.head.sha }}
